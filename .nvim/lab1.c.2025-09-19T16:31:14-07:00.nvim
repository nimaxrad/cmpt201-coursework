#include <stdbool.h>
#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main() {
  printf("Enter your command:\n> ");
  char *buff = NULL;
  size_t size = 0;
  if(getline(&buff,&size,stdin) != -1L){
    
  }
  return 0;
}
